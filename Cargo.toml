[package]
name = "forge"
version = "0.1.0"
edition = "2021"
authors = ["Redstone OS Team"]
description = "Forge - The RedstoneOS Microkernel"
license = "MIT"

[[bin]]
name = "forge"
path = "src/main.rs"
test = false
bench = false

[lib]
harness = false
test = false
bench = false

[dependencies]

[features]
default = []

# =========================================================
# SINGLE PROFILE — KERNEL DEV SAFE
# =========================================================
[profile.release]
panic = "abort"

# 🔒 previsibilidade > performance
opt-level = 1     # NÃO use 0, NÃO use z
lto = false       # LTO embaralha layout
codegen-units = 1 # layout determinístico

# 🧠 debug real
debug = 1      # símbolos úteis (sem exagero)
strip = "none"

# 🚨 segurança durante dev
debug-assertions = true
overflow-checks = true

# ❌ incremental quebra determinismo
incremental = false
