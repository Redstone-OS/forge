[package]
name = "forge"
version = "0.1.0"
edition = "2021"
authors = ["Redstone OS Team"]
description = "Forge - The RedstoneOS Microkernel"
license = "MIT"

[[bin]]
name = "forge"
path = "src/main.rs"
test = false
bench = false

[lib]
harness = false
test = false
bench = false

[dependencies]

[features]
default = []

# =========================================================
# SINGLE PROFILE — KERNEL DEV SAFE
# =========================================================
[profile.release]
panic = "abort"

# 🔒 previsibilidade e performance equilibrada
opt-level = 2     # Nível 2 é o padrão seguro para performance sem quebrar debug agressivamente
lto = false       # Manter false para compilação mais rápida e menos problemas de link
codegen-units = 1 # Determinismo

# 🧠 debug
debug = 2         # Full debug info para backtraces precisos
strip = "none"

# 🚨 segurança
debug-assertions = true
overflow-checks = true

# ❌ incremental
incremental = false
